{
  "name": "sarvam-mcp",
  "user": "ubuntu",
  "install": "bash -lc 'if [ -f .cursor/install.sh ]; then bash .cursor/install.sh; else echo \"[sarvam-mcp] skip install: .cursor/install.sh is not on this git ref. Merge the env-setup PR into main so recurring Cloud Agent builds succeed.\"; fi'",
  "start": "bash -lc 'if [ -f .cursor/start.sh ]; then bash .cursor/start.sh; else echo \"[sarvam-mcp] skip start: .cursor/start.sh is not on this git ref.\"; fi'",
  "terminals": [
    {
      "name": "mcp-inspector",
      "command": "source .venv/bin/activate && mcp dev src/sarvam_mcp/server.py",
      "description": "MCP Inspector for interactive Sarvam tool testing"
    },
    {
      "name": "web-dashboard",
      "command": "cd web && npm run dev -- --hostname 0.0.0.0 --port 3000",
      "description": "Next.js analytics dashboard (health at /health)"
    }
  ],
  "ports": [
    {
      "name": "web-dashboard",
      "port": 3000
    }
  ]
}
